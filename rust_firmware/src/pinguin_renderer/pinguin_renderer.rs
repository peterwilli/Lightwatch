pub struct PinguinRenderer {
    let gui_canvas: &GuiCanvas
}

impl PinguinRenderer {
    pub fn new(gui_canvas: &GuiCanvas) -> Self {
        return PinguinRenderer {
            gui_canvas: gui_canvas
        };
    }

    pub fn r#loop() {
        
    }
}